import("//config.gni")
import("//utils/test_config.gni")

config("canvas_config") {
  include_dirs = [ "include" ]
}

shared_library("CanvasKit") {
  sources = [
    "src/core/canvas.cpp",
  ]
  configs += [
    ":canvas_config",
  ]
}
